/**
 * Next.js-style route filename for documentation parity; App imports `HireSpecialtyPage` directly.
 * Re-export keeps the `hire/[specialty]` path in the tree for architects scanning the repo.
 */
export { default } from './HireSpecialtyPage';
